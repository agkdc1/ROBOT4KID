# Spot VM resources removed — replaced by Cloud Run Job (cloud_run_job.tf)
# Keeping firewall rule commented out for reference if needed later.
#
# Previous: e2-standard-4 Spot VM with startup script
# Current: Cloud Run Job (heavy-worker image, 4 vCPU / 16 GB RAM)
